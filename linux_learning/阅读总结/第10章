1.	使用 type -a name 查询name命令是否
2.	使用 ctrl+u/ctrl+k向前或向后删除命令行，使用ctrl+a/ctrl+e将光标移至最前方/最后方
3.	使用 变量设定有一定的规则，$PATH=${PATH}，参见鸟哥私房菜的429面
		1) 	变量名必须以字母开头，不能是数字或其他
		2)	等号两边不能有空格;在字符中间如果想出现空格，除非使用\反斜杠转译
		3)	使用""则里面使用$变量名时，会自动转变出实际参数，但使用''则仅显示纯文本;
				例如 inf="lang is $LANG" 会显示lang is C 但是inf='lang is $LANG' 会显示 lang is $LANG 
		4)	变量=$(指令)，则可以加入指令提供的信息
		5)	变量需要增加的时候，使用如 PATH=$PATH:/home/bin
		6)	如果，将该变量变成环境变量(在系统运行的时候的可用变量)
		7) 	取消变量的方法，unset
		
4.	使用env   export来查询环境变量的内容
5.	通过export+变量名  将自定义变量输出为全局变量
6.	通过read [-p -t] 变量 可以将键盘的输入读取到变量中 -p为屏幕显示提示语，-t 为限定时间s
7.	declare 可以直接使用显示环境变量、也可以指定自定义变量的数据类型，如整型，也可以加-x将自定义变量转化成环境变量		
8.	变量类型默认的是字符串
9.	变量的删除、修改、增加，查看鸟哥私房菜447面
10.	alias 可以命令已有的命令行
11.	type -a 命令 可以查看该命令的执行顺序，一般先是 ailas 然后再是shell builtin ，最后为bin/xxx 或其他
12.	source（或者.） +配置文件   读取配置文件(不需要注销或者关机)
13. 使用ctrl+s 是执行stop，即屏幕不执行输出，此时可使用ctrl+q退出stop程序
14.	数据流重导向，其中 > 是取代，将以前的数据抹去，写入新数据; >> 是累加，直接在原数据下面叠加， > 默认为1,即 1> 标准输入   2> 标准错误输入
