1.	下达命令的格式：command [-options] parameter1 parameter2 ... 
										命令				选项					参数1			参数2
		命令可以是命令 或者可执行文件(如批次脚本，script)
		选项：一般带符号-;当然也可以使用选项完整名称，带符号--
		命令，选项，参数之间是使用空格隔开，无论使用多少空格，shell都将其视为1个
		一般，使用enter可以直接开始执行命令，但\反斜杠可以跳脱enter符号，使指令连续到下一行，即使用\+enter跳到下一行
		
2.	该章节有讲到关机、在线帮助和离线帮助，以及文档编辑器
